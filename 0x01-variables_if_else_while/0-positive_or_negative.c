#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
 * generate rondom numbers
 */
int main()
{
int n;
srand(time(NULL));
if n < 0
{
printf("%d\n is negative", rand());
}
else if n > 0
{
printf("%d\n is positive", rand());
}
else n == 0
{
printf("%d\n is zero", rand());
}
return 0;
}
